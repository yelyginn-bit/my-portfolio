# Аудит внутренних ссылок

Дата аудита: 22.08.2026. Ссылки сверены с общим route manifest (`src/public/routeManifest.ts:21-75`) и redirect-правилами production server (`server/production-server.js:75-97`). По условиям фазы 2 исправлена только согласованная ссылка на фотопортфолио в `/ceny`.

| URL | Откуда ведёт | Статус | Действие в фазе 2 |
|---|---|---|---|
| `/portfolio/photo` | `photo.html:180`; `src/App.tsx:376`; `src/lib/pricing.data.ts:25-26`; `src/config/site.ts:89` | Маршрут существует, но намеренно закрыт и не имеет публичного контента. Для публичной навигации это тупик. | Не менялся: владелец разрешил заменить только ссылку на `/ceny`; остальные источники зафиксированы для отдельного решения. |
| `/portfolio/photo` | `ceny.html` (до фазы 2) | Публичная ссылка вела в закрытый пустой маршрут. | Исправлено на `/photo` (`ceny.html:137`). |
| `/portfolio/editing` | `ceny.html:117`; `src/App.tsx:377,1781,1850`; `src/config/site.ts:70-80` | Категории `editing` нет. Production делает постоянный redirect на `/portfolio/post` (`server/production-server.js:87-89`), поэтому ссылка не даёт 404, но является устаревшей. | Не менялась по прямому указанию владельца; рекомендуется отдельная замена на `/portfolio/post`. |

Других внутренних ссылок, ведущих в несуществующий маршрут без server redirect/fallback, в активных публичных страницах не найдено. Ссылки `/prices` и `/cases` считаются устаревшими алиасами, но корректно обслуживаются постоянными redirect (`server/production-server.js:75-85`). Ссылки на `/account`, `/admin`, `/gallery`, `/journal` являются намеренными внутренними маршрутами, а не битыми ссылками.
