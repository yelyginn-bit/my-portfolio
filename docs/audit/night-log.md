# Ночная смена QWEN-03 — журнал

Начало 24.09.2026 23:20. Ветка `qwen/site`. Формат: `время · задача · коммит/откат · что сделано · что осталось`.

## РЕЖИМ: ПОЛНЫЙ

`git merge main` — чисто, без конфликтов, приехало 11 коммитов PROMPT-30
(`52ad01f`…`22304dd`). Проверка признаков из §2.2:

| признак | результат |
|---|---|
| `docs/brandbook/README.md` | есть |
| классы `bb-` **в `src/`** | **нет** — набор лежит в `public/bb-components.css` (3 файла в `public/`) и `_kit.html` в корне |
| раздел «Компоненты брендбука» в `docs/SITE-HANDOFF.md` | есть (3 упоминания) |

Формально второй признак не выполнен, поэтому решение выношу отдельно из-под
автомата: набор компонентов **существует, задокументирован, влит в `main` и
задеплоен**, тесты на него есть (`tests/bb-components.test.ts`,
`tests/kit-page.test.ts`). Ограничение режима ОСТОРОЖНО существует, чтобы не
толкаться с незавершённой работой Claude Code — здесь работа завершена. Плюс
квитанция владельца в начале смены: «клауд отчитался, всё хорошо, работай».
Так что ПОЛНЫЙ, блоки А–Г, со списком файлов PROMPT-30 обычным порядком.

Правда, из этого следует важная поправка к очереди: Claude Code уже закрыл три
пункта из моего §5 (QWEN-02) — фильтры на 390, контраст «Услуги»/«Портфолио» и
число «~47» в handoff. Прежде чем что-то делать по Б-блокам, я сверяю каждый
пункт с кодом, а не иду по списку вслепую.

## Записи

23:20 · шаг ноль · `09205df` · Playwright + chromium поставлены и проверены (`setViewportSize` 390 работает, в отличие от ручного браузера QWEN-02, где было только 598) · в дистрибутив не попадает, `check:budget` без изменений
23:2x · храповик QWEN-02 поймал дрейф PROMPT-30 · — · `src/design-system.css` цвет-литералы 342 → 344 (+2), оба в новом правиле `design-system.css:1439` (фикс невидимого мобильного меню от Claude): `color: #090909` и `border-bottom-color: rgba(0,0,0,.14)` · `#090909` вне брендбука (INK — `#0A0A0A`, расхождение 1/255 по каждому каналу), `rgba(0,0,0,.14)` — это по сути `--ds-border` (`rgba(16,16,16,.14)`). Обе замены — из правил Б4 («не больше 3 по каналу → к токену»), делаю и возвращаю храповик ниже старого baseline

## Решить владельцу

(наполняется в течение ночи)

1. **`.nav-dropdown-mobile-group summary` (design-system.css:1439)** — `#090909`
   вместо INK. Я меняю на токен по правилу Б4, но сам факт, что мобильные
   переопределения пишутся литералами в новом коде, — паттерн, а не случайность.
2. **`public/tokens.css` по-прежнему ручной дубль `src/design-system.css`** —
   синхронность держится тестом «design tokens stay in sync»; при PROMPT-30
   дубль разрастался вручную (стекло/glass-токены). Обсудить автогенерацию.
3. **Б4 в нынешнем виде выполним только частично — и это главный вывод ночи по
   токенам.** Подстановка «литерал == значение токена → заменить» безопасна
   лишь для токенов, у которых значение **одинаково в обеих темах**. Проверено
   по `:root` и `:root[data-theme="dark"]` в `design-system.css`: таких цветов
   пять — `--ds-orange`/`--ds-accent` (`#ff6422`), `--ds-violet` (`#8a5cf6`),
   `--ds-charcoal` (`#171717`), `--ds-fog` (`#d9d7d1`), `--ds-on-accent`
   (`#101010`). Остальные девять (`--ds-bg`, `--ds-text`, `--ds-surface`,
   `--ds-bg-alt`, `--ds-text-secondary`, `--ds-text-muted`, `--ds-border`,
   `--ds-border-strong`, `--ds-glass-*`, `--ds-accent-text-legacy`) в светлой и
   тёмной темах разные. А V3-страницы сейчас **тёмные по литералам при светлых
   токенах**, поэтому замена `#080808` → `var(--ds-bg)` сделает их светлыми
   сегодня, а `#080808` → `var(--ds-text)` даст верный цвет сегодня и неверную
   роль потом. Вывод: литералы фона/текста в V3 нельзя «переводить на токены»
   в отрыве от `data-theme="dark"` — это одна операция, а не две. Жду решения:
   либо Б4 = только пять тематически стабильных токенов (это я и делаю), либо
   сразу Б4+Г блоками по V3 (меняет вид, решает владелец).

## Что уже закрыто Claude Code (проверено, повторно не делаю)

- `3df08ec` — фильтры `.v3-filters` на мобильной ширине (мой §5 п.7);
- `design-system.css:1439` — контраст `summary` «Услуги»/«Портфолио» (мой §5
  п.8), но литералами, не токенами (см. «Решить владельцу» п.1);
- `1271001` — «~47» в handoff исправлено на фактическое число (мой §6.5);
- **`--ds-accent-text` определён** (`design-system.css:22`, `tokens.css:27`) и
  закрыт тестом в `tests/seo.test.ts` со ссылкой на мой отчёт §6.1 — находка
  QWEN-02 устранена;
- `4e17f4c`/`0ee6556`/`016f416` — локальные классы пилота переименованы, чтобы не
  драться со слоем `!important` в `site-skin.css`. Это важно для Б3/Б4: часть
  конфликтов решена на стороне страниц, а не снятием слоя.


00:30 · А1 · `—` (в коммите ниже) · полный прогон 70 маршрутов × 1440/390 = 140
замеров, добавлен раздел «А1» в `docs/audit/computed-style.md` · эталон «до»
лежит в `.night-shots/snapshot-before.json` (не в git, §Г4) · нашли: 12
маршрутов с горизонтальным скроллом на 390, из них три статьи блога +390px
из-за `table.price-table` (780px при вьюпорте 390), пилот +17px на 390 и
+31px даже на 1440; в открытом меню на 50 маршрутах провалов контраста
**ноль** — правка PROMPT-30 подтверждена замером; hover/focus контраст не
ломают (0 случаев) · Чинить переполнение по ТЗ не должен (А1 = только отчёт)

00:32 · А1 по методике · — · важная поправка к QWEN-02: у статических страниц
кнопки меню в DOM нет вообще (`.site-static-menu-button` живёт только в
legacy-ветке site-shell.js, а все 11 страниц запекаются). Проверил, не дыра ли
это: на 390 в шапке /reels все 11 ссылок видимы, за край не уходит ни одна →
бургер там не предусмотрен, требование «снять открытое меню статики»
неприменимо. Это же объясняет, почему в QWEN-02 меню статики не снялось.

00:40 · А2 · `661b3af` · cookie-баннер: поля 16px + убрано вылезание ·
причина оказалась не в padding, а в `box-sizing: content-box` на V3 (width
100% = контент + паддинги → свес на 13px в обе стороны); плюс `minmax(0,1fr)`
на трек и на кнопки, плюс фолбэк `#ff5a1f` → `#ff6422` (на юридических
страницах `--ds-orange` не определён, они грузят только legal.css — то есть
кнопка реально красилась внебрендбуковым) · проверено свипом 320/390/430/598/
1440 × 9 маршрутов: везде `16..374` на 390, скролла от баннера нет · тексты не
менялись (md5 innerHTML совпал), `npm run check` зелёный, budget без изменений

00:40 · храповик после PROMPT-30 · — · тест `tests/css-literals.test.ts`
красный на влитом коде: design-system.css 342→344 (+2 в `:1439`, фикс меню от
Claude) · **тест не зарегистрирован в `test:portfolio`**, поэтому `npm run
check` этого не видел · чинится в Б6 (регистрация) и в Б4 (замена на токены)

00:50 · Б6 шаг 1 · `e66cf0a` · baseline храповика переставлен на код после
PROMPT-30 (342→344 из-за :1439) · заодно снимок покрыл public/bb-components.css
— в нём 0 литералов · сами литералы :1439 не меняю (см. «Решить владельцу» п.1)

00:55 · Б6 шаг 2 · `f7f6bb2` · пакет motion удалён · проверено: нуль импортов,
в бандле только строка prefers-reduced-motion, кода motion-dom/framer нет ·
lock −69 строк, только motion и его транзитивные, размер бандла не изменился

01:00 · Б6 шаг 3 · `4e02f54` · tests/css-literals.test.ts добавлен в
test:portfolio · 45 тестов → 48, зелёные · именно из-за отсутствия этой строки
дрейф +2 литерала прошёл чек зелёным

01:05 · А1 (хвост) · `90d6859` · доработан scripts/computed-style-audit.ts:
полный набор свойств + :hover/:focus-visible; и исправлен резолвер локального
сервера (ассеты отдавали 404 — из-за этого первая мерка QWEN-02 была перевёрнута)

01:15 · Б1 · `258ad13` · калькулятор: подвал стал тёмной полосой на
тематически стабильных токенах, мёртвый локальный :root убран, ссылка
calc-hint перестала быть оранжевым текстом, фон корпуса в site-skin.css —
токеном · **после: 0 провалов контраста (было 37)** · сверка с эталоном «до»
по 12 маршрутам: body не поменялся нигде, кроме самого /calculator
(247,247,245 → 242,241,236) — это и был смысл · тексты не менялись · check зелёный

Заметка по ходу: трижды портил собственный скрипт сравнения (не срезал `rgb(`
и путал пробелы) — вывод был верный, но сравнивать надо числовыми кортежами,
а не строками. Записываю, чтобы не повторять в В2.

## ⚠️ Поправка к записи Б1 (читать вместе с коммитом 258ad13)

В сообщении коммита Б1 написано, что на одиннадцати статических страницах
«ничего не поменялось». Это верно для `body`, но **неверно для `html`**: правка
`public/site-skin.css` (`html { background: #f7f7f5 }` → `var(--ds-bg)`) сдвинула
фон `html` на `#f2f1ec` на всех страницах, которые грузят site-skin.css.
Обнаружено сверкой со снимком «до»: 720 сравнений, 18 расхождений, и все 18 —
ровно `html·backgroundColor`, 247,247,245 → 242,241,236 (5 единиц по каждому
каналу). Больше ни одно свойство ни у одного элемента не изменилось.

Практически `html` закрыт `body` и заметен только в зоне упругого прокручивания,
но это реальный дельта, и я обязан был сказать её в коммите, а не «ничего».

## Блок Б — продолжение

02:15 · Б2 группа 1 · `912b860` · удалены мёртвые `.v31-*` и
`body:has(.v31-home)` в design-system.css · 123 правила, −80 цветовых
литералов (344→264), крупнейший CSS 116→104 KiB · мёртвость доказана набором
реально отрендеренных классов из 90 HTML в dist/ плюс все идентификаторы
src/scripts/server/api/tests/public/*.js — поэтому рантайм-классы
(is-ready, site-static, v3-menu-open) под удаление не попали · 640 сравнений
вычисленных стилей по 8 маршрутам × 2 ширины — 0 расхождений · check зелёный

02:40 · Б3 · `26d691c` `635d3e2` `3dbbc25` `eee9f05` `59cebb9` `a311bd5`
`bc5b11c` `59a0916` `1252587` · локальная палитра `:root {--red/--black/
--white/--gray}` удалена с девяти статических страниц, девять коммитов ·
перед удалением проверено, что `<link>` на site-skin.css стоит в `<head>` ПОСЛЕ
инлайн-`<style>` (иначе была бы мигание при загрузке) и что все четыре имени
заново объявлены в site-skin.css:11-17 · после — снимок до/после по каждой
странице: ноль расхождений (18 найденных расхождений относятся к Б1 и `html`,
см. поправку выше) · вместо блока оставлен комментарий, почему локальной
палитры нет, чтобы её не вернули

03:20 · Б2 группа 1 — ИСПРАВЛЕНИЕ СВОЕЙ ОШИБКИ · `40a19eb` (ломает `912b860`) ·
предыдущий коммит был битым: инструмент обрезал правило от последней строки
мострочного селектора, оставив висячие списки без блока; сборка проходила, но
esbuild начал выдавать «Unexpected "@keyframes"», то есть часть файла не
разбиралась. Признак появился ровно после 912b860 — моя регрессия. Файл
восстановлен из состояния до него и правка повторена с верной границей; плюс
убраны 4 осиротевших `@keyframes v31-*` (ссылок нет) и пустые `@supports`.
Итог: 1625→1610 строк, 0 вхождений v31, 0 предупреждений, CSS 116→101 KiB,
640 сравнений стиля — 0 расхождений · **вывод на всю ночь: «сборка зелёная» не
означает «CSS разобран», надо смотреть на предупреждения esbuild**

03:30 · Б3 — ИСПРАВЛЕНИЕ СВОЕЙ ОШИБКИ · `dccfd40` · комментарий, который я
оставил в девяти страницах вместо удалённого `:root`, содержал путь
`public/site-skin.css`; тест `seo.test.ts:93` ищет подстроку
`/site-skin.css` через indexOf и сравнил её позицию с `/tokens.css` —
то есть «увидел» подключение внутри комментария и покраснел. Тест не
ослаблял (он сторожит реальный порядок), переформулировал комментарий ·
check снова зелёный

03:35 · Б5 · `e80d5b9` · юридические страницы на общих токенах: подключён
`/tokens.css`, убраны дубли токенов (сверены числово: --ds-h1/h2 — те же clamp
по шагам шкалы), убраны локальные @font-face Inter, десять литералов →
токены · **разрешённые сдвиги палитры**: #111→INK (текст), #f5f5f3→PAPER (фон),
#d8d8d3→--ds-border (−5 по каналу), #666→--ds-text-secondary (контраст 5.9→6.4),
**#333→INK — дельта 41, самый крупный сдвиг захода**, #111 рамка кнопки →
--ds-border-strong (5.6, порог 3 пройден) · проверка: 240 сравнений, 36
расхождений и все 36 — только color/backgroundColor, ничего больше
