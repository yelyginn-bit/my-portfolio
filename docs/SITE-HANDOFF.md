# SITE-HANDOFF — yelyginn.ru

Единый источник правды о состоянии сайта. Живёт в репозитории в `docs/` и обновляется
**вместе с кодом**: задача, которая меняет сайт, в том же заходе обновляет этот файл.
Версия от 24.09.2026 пересобрана заново — прежняя, от 23.08, разошлась с кодом на месяц.

## Архитектура и деплой

- React 19, TypeScript, Vite 6, Express в продакшене. Роутинг ручной (`V3App` читает
  `window.location.pathname`), router-библиотек нет.
- Пре-рендер на сборке (`react-dom/server` + `hydrateRoot`). **Маршрута нет в пре-рендере —
  у него нет H1 и меты для робота.** Готовый HTML лежит в `dist/prerender/<маршрут>/index.html`.
- REG.RU VPS → nginx → PM2 (`yelyginn-site`, fork) → Express → `dist/`.
- `deploy.sh`: git pull → npm ci → build → проверка свежести пре-рендера → H1-гейт →
  `pm2 restart` → финальный H1-loop. Доступ по SSH-ключу.
- Репозиторий: `yelyginn-bit/my-portfolio`.

## Источники истины

- Цены — `src/lib/pricing.data.ts`. Нигде больше числом не писать.
- Проекты портфолио и кейсы — `src/portfolio/v3PortfolioData.ts` (поле `caseStudy`).
- Навигация — `src/lib/navigation.data.ts` (PROMPT-21 §3): пункты шапки, мобильного
  меню и подвала всех систем берутся отсюда, маршрут проверяется по route manifest
  на сборке — ссылка на несуществующий или приватный маршрут валит сборку.
- Дизайн — брендбук в Figma (`p3IBlfFFdnA03s3U7UiarF`), а не то, что сейчас на сайте.
  Шесть цветов: INK `#0A0A0A`, PAPER `#F2F1EC`, GRAPHITE `#171717`, FOG `#D9D7D1`,
  VIOLET `#8A5CF6`, SIGNAL ORANGE `#FF6422`. Подписи на оранжевом — только INK.
- Маршруты — `src/public/routeManifest.ts` (route manifest); `sitemap.xml` — только
  артефакт сборки.
- Видео на страницах проектов — один плеер, `KinescopeEmbed` (`src/components/media/`):
  ленивый iframe, IntersectionObserver, играет на сайте. Второй механизм (`LazyPlayer`,
  уводил на kinescope.io во внешней вкладке) удалён PROMPT-25.

## Домены и внешние сервисы — карта путаницы (сверено в августе, перепроверять при работе с формой)

Это самое запутанное место в проекте, держи в голове при любой работе с формой или БД:

| Что | Где | Статус |
|---|---|---|
| Сайт (весь HTML, статика) | REG.RU VPS | Работает, это единственный источник правды для контента |
| Форма `/api/send-form` | Vercel, проект `yelyginn-v3` | Работает через nginx proxy_pass. НЕ автодеплоится из GitHub — деплоится вручную `vercel deploy` |
| Supabase `supabase-bistre-bell` | Подключён к проекту `my-portfolio-github-copy` (Vercel) | Приостановлен (free tier). НЕ используется рабочей формой — это мёртвая ветка от заброшенного клона |
| Домен `yelyginn.ru` в панели Vercel | Числится привязанным к `my-portfolio-github-copy` | Не влияет на реальный трафик — DNS ведёт на VPS, но привязку стоит когда-нибудь отвязать вручную |
| Проекты `my-portfolio`, `ai-studio-zip-project` | Vercel, автодеплоятся из того же GitHub-репо | Не используются в проде, лишний шум при каждом push |
| `yelyginn-current-preview`, `yelyginn-v3-preview` | Vercel, появились 14.08, без Git-привязки | Причина появления не выяснена, не критично |

**Вывод: при любой работе с ценами/формой/БД — источник истины всегда `yelyginn-v3` на Vercel и VPS-репозиторий. Всё остальное — шум.**

## Текущее состояние

Единая картина: что сделано, что в работе, что осталось. Обновлять здесь,
а не заводить новые заметки со списками — иначе разойдутся, как разошлись
`SITE-HANDOFF.md` и `TASKS.md` в папке `site` (они устарели на месяц,
не читать как источник истины, см. хвост заметки).

**Прод:** последний деплой этого захода — [commit] `15eff07` (фаза 3 — шрифты),
24.09.2026. В этот же заход до него на прод ушли подряд: навигация (единый
источник, впечена в 11 статических страниц + V3 + Layout + LegalApp +
калькулятор, кликабельность, `tests/navigation.test.ts`),
`src/config/routes.ts` (мёртвый второй источник маршрутов, удалён), кейс HOFF,
блоки КВН и мастер-класс на `/photo` (включая фикс: три отдельные ленты
лайтбокса вместо одной на 61 кадр, единая кнопка заявки), один видео-плеер
`KinescopeEmbed` на всех 38 страницах проектов (`LazyPlayer` удалён), валидатор
реестра переведён на живые данные (`v3PortfolioData.ts`, было — мёртвый
`portfolio.data.ts`), весь механизм `/cases/<slug>` удалён (`PUBLISHED_CASE_SLUGS`
был пуст — ни одной страницы не порождал; редирект `/cases → /portfolio`
сохранён), кейс KORONA, блок «Преподавание и телевидение» на «Обо мне»,
`docs/SITE-HANDOFF.md` перенесён в репозиторий, и фаза 3 — единая система
шрифтов (Inter self-hosted, шкала 104/72/48/32/20/16/12/11, все три жалобы
владельца закрыты и подтверждены замером: заголовок блога был 4 строки на
1440px — стал 2; `/ceny` весь текст, включая цену, был `ui-monospace` — стал
Inter; `/photo` было мимо общей системы — теперь на токенах). Заодно нашёл
и убрал «Arial Narrow» — второй, не названный в промпте display-шрифт в
9 местах V3 (главная, футер-wordmark, карточки работ), который не установлен
на macOS/iOS и уже был источником задокументированного бага с обрезкой
текста на живом iPhone. Всё подтверждено прогоном `deploy.sh` (H1-гейт
зелёный по всем маршрутам) и живой проверкой на проде после каждого шага.

**Доступ:** SSH-ключ установлен, Claude Code деплоит сам, пароль больше не нужен.
Root по паролю через SSH невозможен by design (`PermitRootLogin without-password`) —
это не поломка, а настройка, см. «Уроки и грабли».

---

## Сделано

### Инфраструктура и деплой
- Пре-рендер (build-time SSG) на все публичные маршруты. **Главное правило проекта:**
  маршрута нет в пре-рендере — у него нет H1 и меты для робота, страницы для поиска
  не существует.
- `deploy.sh`: git pull → npm ci → build → проверка свежести пре-рендера →
  H1-гейт → `pm2 restart` → финальный H1-loop. Гейт зелёный по всем маршрутам.
- nginx приведён в порядок (`sites-available` + симлинк, дубли убраны).
- SSH-ключ, автономный деплой.
- Настоящая страница 404 вместо мягкого 404 (`fe49456`): раньше несуществующий
  URL отдавал 200 и главную — поисковик индексировал мусор.
- `sitemap.xml` — только артефакт сборки, из git убран. Генерируется из route manifest.
- `/gallery` вычищен из манифеста (76 → 75 маршрутов): маршрут числился, страницы не было.
- `src/config/routes.ts` — мёртвый параллельный источник маршрутов (расходился
  с реальным манифестом, например по `/photo`) удалён.

### SEO и машиночитаемость
- `/ceny` заведён в пре-рендер. **Самая ценная правка месяца:** до неё 7 цен
  из 17 робот не видел вообще, включая все три позиции по трансляциям.
- Дубли title/description разобраны: настоящий дубль был один (8 юридических URL),
  остальное — похожие, но не идентичные.
- `/photo` открыт для индексации (был закрыт тремя способами одновременно).
- H1 и мета для восьми юридических страниц.
- `priceRange` на `reels.html` и `event-video.html` исправлен на основную услугу страницы.
- Лишний H1 на `/ceny` убран.
- **Навигация впечена в HTML на сборке** для 11 статических страниц, V3, Layout,
  LegalApp, калькулятора — до этого у 11 страниц (все семь посадочных услуг в
  их числе) шапку вставлял `site-shell.js` в рантайме, робот без JS не видел
  навигацию вообще. Замер `curl` без JS после фикса: 57–69 внутренних ссылок
  в сыром HTML на каждой из этих страниц (было 0).
- `/portfolio/photo` → редирект 301 на `/photo`. Мёртвый артефакт `blog/index.html`
  снесён.

### Навигация (PROMPT-21 / 21b, «фаза 4»)
- Единый источник — `src/lib/navigation.data.ts`: шапка, мобильное меню и подвал
  всех систем берут пункты отсюда, маршрут — ключом из route manifest, а не строкой.
- Структура шапки: Услуги (выпадающее, 7 посадочных) / Портфолио (выпадающее,
  все 10 категорий) / Фото / Цены / Блог / Обо мне, справа — калькулятор и CTA.
  Раньше «Съёмка»/«Пост» были отдельными пунктами — теперь внутри «Портфолио»
  вместе с остальными восемью категориями. «Цены» раньше не было в шапке ни
  одной из систем — теперь есть.
- Подвал — карта сайта по группам (Услуги / Работы / Цены и расчёт / Блог /
  О себе и контакты / Документы), у калькулятора появился подвал (раньше не было).
- Кликабельность (этап В): «похожие работы» на 7 посадочных, «соседние кейсы» +
  ссылка на услугу на всех страницах проектов, услуга + калькулятор в конце
  4 статей блога. Ссылка на калькулятор вынесена из блока «Соседние кейсы»
  в отдельную строку рядом с ним (PROMPT-25 §4) — калькулятор не кейс.
  Связи берутся из данных (`CATEGORY_TO_SERVICE`, категории проекта), не
  прописаны руками в разметке.
- `tests/navigation.test.ts` — регресс: каждый публичный индексируемый маршрут
  есть в подвале, в навигации нет маршрутов не из манифеста и непубличных,
  калькулятор и «Фото» есть в шапке. Без захардкоженных чисел.
- Выпадающие меню шапки — реальные `<a href>`, открываются по клику/тапу,
  рендерятся через `createPortal` в `document.body` (были обрезаны
  `overflow:hidden` у `.v3-nav`, который стоит ради скруглённых углов).
- `AGENTS.md` поправлен: «Фото» — публичный продукт навигации (было наоборот).

### Видео и реестр (PROMPT-25)
- Один плеер на весь сайт — `KinescopeEmbed` (ленивый iframe,
  IntersectionObserver, 200px rootMargin, играет на сайте). `LazyPlayer`
  (заглушка-ссылка на kinescope.io в новой вкладке, стоял на 37 из 38 страниц
  проектов — уводил посетителей со всех этих страниц) удалён. В `KinescopeEmbed`
  перенесены недостающие черты: постер на заглушке, ограничение ширины
  вертикальных (9:16) роликов.
- Валидатор реестра (`validatePortfolioRegistry`) переведён на живые данные
  (`v3PortfolioData.ts`) — раньше проверял мёртвый `portfolio.data.ts`, из
  которого не строится ни одна страница. На живых данных дублей Kinescope ID
  между проектами не найдено.
- Весь механизм `/cases/<slug>` удалён: `cases.html`, `case.html`,
  `CaseRouter.tsx`, `CasePage.tsx`, `PUBLISHED_CASE_SLUGS`/`CASE_PROJECTS`
  (список публикаций был пуст — ни одной страницы не порождал),
  `src/lib/portfolio.data.ts` целиком. Редирект `/cases → /portfolio` сохранён
  (сервер 301-ит раньше, чем отдалась бы статика).

### Кейсы портфолио (обогащённый шаблон, PROMPT-23/26)
- HOFF (`/portfolio/hoff-product-cards`) и KORONA (`/portfolio/korona-production`)
  — поле `caseStudy` (Задача/Моя роль/Как сделано/Результат) в
  `v3PortfolioData.ts`, текст дословно от владельца.
- Очередь дальше: Sber Архитектура (ждёт ответа владельца — какие 3 из 6
  роликов курса монтировал Юрий), ГРК, Альметьевск, СИБУР — по одному кейсу
  на промпт. VK Fest — страницы не существует (владелец был оператором прямой
  трансляции, не монтажёром отдельного ролика). «Учёные Нижнего» — после
  согласования цвета заказчиком.
- Для справки — разрешение двух исторических дублей Kinescope ID: ID
  `iXmVYXkXdmFiHpn6NCNoyq` — «Горький в тени войны» (не VK Fest, той страницы
  нет), `fvxndmGGHqWtuCcK5TnB4j` — тизер «Женщины СИБУРа» (не «Основа»).
  Проверено PROMPT-25 §2 на живом реестре — сейчас дублей нет вообще, каждый
  ID закреплён ровно за одним проектом.
- Отдельно сверена страница «Горький. Страницы памяти» (`gorky-stranicy-pamyati`,
  другой спектакль, не «Горький в тени войны»): видео `2PzbBYe3Xb3XQSGCZH3kMn`,
  роль — только «operator» (съёмка), монтаж не приписан. Совпадает с тем, что
  подтвердил владелец — править не пришлось (PROMPT-27 §3).

### Контент
- Фундамент портфолио: типизированный реестр, ленивый Kinescope-эмбед 16:9 и 9:16,
  валидатор целостности с падением сборки, публикация по явному списку слагов.
- Фотопайплайн: 87 файлов, только webp, 38 МБ → 17 МБ, EXIF снят.
- `/cvetokorrekciya` — 10 пар в трёх группах, авторская подпись по СИБУРу.
- `/photo` — галерея 41 кадра плюс блоки «Репортаж и бэкстейдж: КВН» (12 кадров)
  и «Мастер-класс: Школа джазовых блогеров» (8 кадров). Лайтбокс — три отдельные
  ленты (data-group на кнопках): основная галерея / #reportazh / #masterklass,
  переход между лентами закрыт, листание внутри своей ленты по кругу. Кнопка
  заявки «Фотограф на ваше мероприятие» — одна, после обоих блоков (раньше
  стояла между ними).
- `/pryamye-translyacii` — посадочная с реальными ценами, живая.
- «Социальный участковый» опубликован с честной ролью «свет» (заведён новый `RoleTag`,
  без фантомных маршрутов).
- Блок «Преподавание и телевидение» на «Обо мне» — текст владельца дословно,
  без сканов и картинок.

### Вёрстка
- Мобильный заход целиком — см. «Мобильная вёрстка — итоги».
- Плашка кук: заголовок 42.9px → 16px, высота 30.5% → 22.8% экрана 390px.
- Бегущая строка заказчиков: доказано, что это не регресс, добавлена маска затухания
  для режима «уменьшить движение».

### Решения по стилю
- **Источник истины по дизайну — брендбук в Фигме, а не сайт.** До 21.09 стиль
  реверс-инжинирился с непоследовательного сайта, отсюда две неверные установки
  («эталон — главная», «заголовки Helvetica Neue»), обе отозваны.
  См. «Брендбук — источник истины».
- Тема сайта — **тёмная везде**, выбрано владельцем.
- Палитра: решение принято 22.09, пять красных сводятся к SIGNAL ORANGE,
  мелкий акцентный текст на светлом остаётся до перевода страницы на тёмное.
  `#a58cff` (`.v3-catalog .v3-contact__intro a`) — расследован: селектор мёртвый,
  ни на одной живой странице не совпадает (`ContactSection` везде рендерится
  как `.v3-contact--unified`), фона для замера не существует — не трогали,
  задокументировано в `tests/palette.test.ts` как известный мёртвый код.
- Тёмная тема — механизм (PROMPT-29): `:root[data-theme="dark"]` в
  `design-system.css`/`public/tokens.css` переопределяет те же токены, что
  задают светлую тему; страницы просто их используют, ничего про тему не
  знают. Список тёмных страниц — `ROUTE_MANIFEST`, поле `theme: "dark"`.
  Пилот `/video-dlya-marketpleysov` переведён, локальная симуляция тёмной
  темы (свой `#id {--red/--black/--white/...}` блок) убрана. Шапка/подвал
  статических страниц не реагируют на тему и не должны — это фиксированный
  хром (светлая шапка-пилюля, тёмный подвал) одинаковый на всех страницах
  независимо от темы, подтверждено на живой светлой странице.

---

## В работе

**Фаза 2 (палитра), фаза 4 (навигация) и фаза 3 (шрифты) закрыты и на проде.**
Порядок фаз:

**2 палитра → 4 навигация → 3 шрифты → 5 тёмная тема**

**Фаза 3, шрифты (`PROMPT-28-shrifty.md`) — сделано 24.09, `15eff07`+`5a6fef9`.**
Единая система по брендбуку (Figma `p3IBlfFFdnA03s3U7UiarF`, «02 / TYPOGRAPHY»):
Display/Body — Inter (self-hosted, `public/fonts/inter/`, 400/500/700 ×
cyrillic/cyrillic-ext/latin/latin-ext, лицензия OFL рядом), Mono — оставлен
системный стек по решению владельца. Токены `--ds-font-display/body/mono`,
`--ds-fs-104…11`, `--ds-track-display/h2`, `--ds-fw-display` (700 — «Inter Bold»,
не «чем жирнее тем заметнее», хвост этой фазы закрыт уже в PROMPT-29 §1). Все
три жалобы закрыты и подтверждены замером — см. «Сделано» выше.

**Фаза 5, тёмная тема (`PROMPT-29-temnaya-tema-pilot.md`) — механизм + пилот
сделаны 24.09.** Токены `--ds-bg/--ds-text/--ds-surface/...` переопределяются
под `:root[data-theme="dark"]` в `design-system.css`/`public/tokens.css`.
Список тёмных страниц — `ROUTE_MANIFEST` (`src/public/routeManifest.ts`), поле
`theme: "dark"`, единственное место в коде; `tests/theme.test.ts` проверяет,
что каждая такая страница отдаёт `data-theme="dark"` в собранном HTML и что
никакая другая страница не отдаёт его случайно. Пилот —
`/video-dlya-marketpleysov`. Главная НЕ переведена (промпт прямо запретил её
трогать в этой фазе) — она тёмная, но собрана до токенов, ждёт отдельного
захода. Тёмной темой по умолчанию сайт не стал — это осталось только
механизмом + одной страницей, остальные светлые страницы не тронуты.
Инструкция для перевода следующей страницы — раздел «Тёмная тема: как
перевести следующую страницу» ниже.

---

## Тёмная тема: как перевести следующую страницу

Читает вторая модель. Перевод страницы = атрибут + уборка её локальных
хардкодов, без нового CSS (если пришлось писать новый CSS под тёмную —
значит, хардкод не найден, ищи ещё).

1. **Атрибут.** `<html lang="ru" data-theme="dark">` в самой странице (статика)
   или добавить `theme: "dark"` в её запись `ROUTE_MANIFEST` (V3/прочее — тогда
   атрибут проставит `scripts/prerender.ts`, функция `applyTheme`, сама).
   Плюс `<meta name="theme-color" content="#0A0A0A">` в `<head>` (для V3-страниц
   тоже `applyTheme`, руками не дублировать).
2. **Найди локальные хардкоды.** Обычно это: (а) свой `:root`/`#id`-блок,
   заново объявляющий `--red`/`--black`/`--white`/`--site-bg`/`--site-text`
   и подобные легаси-имена под тёмный вид одной страницы — удалить целиком;
   (б) буквальные hex/rgb в инлайн `<style>` — искать `#[0-9a-f]{3,6}` и
   `rgba(0,0,0` / `rgba(255,255,255` с НЕ-декоративной ролью (текст, заливка,
   граница контрола — не тонкий разделитель) — заменить на `var(--ds-*)`.
   Ролевая таблица (значения — в design-system.css `:root[data-theme="dark"]`):
   фон → `--ds-bg`, поверхность/карточка → `--ds-surface`, текст → `--ds-text`,
   вторичный текст → `--ds-text-secondary`, акцент → `--ds-orange`, подпись на
   заливке акцента → `--ds-on-accent`, декоративный разделитель →
   `--ds-border`, граница контрола → `--ds-border-strong`.
3. **Скругления.** Только у стекла (`backdrop-filter: blur`). Всё остальное —
   `border-radius: 0` (брутализм, §3 промпта). На пилоте это тоже нашлось и
   поправлено — не только цвет.
4. **Шапка/подвал сами не перекрашиваются и не обязаны.** Проверено на живой
   светлой странице: подвал `.site-static-footer-shell` уже `#080808`/PAPER
   на ВСЕХ статических страницах, шапка `.site-static-header` уже светлая
   пилюля на всех — это фиксированный хром независимо от темы тела страницы,
   тот же паттерн, что у V3. Не чинить, если только не найдётся страница, где
   это внезапно не так.
5. **Тесты.** Добавить файл страницы в `SCANNED_FILES` `tests/palette.test.ts`
   (литералы hex вне брендбука); если встретилась НОВАЯ комбинация
   цвет-на-фоне — добавить пару в `scripts/contrast.ts` `PAIRS` с ролью
   (`text`→4.5 или `large-ui`→3.0) и её реальным контрастом (запусти
   `npx tsx -e "import {contrastRatio,BRAND} from './scripts/contrast.ts'; console.log(contrastRatio(BRAND.X,BRAND.Y))"`,
   не изобретай число). `tests/theme.test.ts` подхватит страницу из
   `ROUTE_MANIFEST` сам.
6. **Проверка.** `npm run build`, затем `getComputedStyle` на живой (или
   собранной) странице: фон, текст, вторичный текст, кнопка (фон+подпись),
   ссылка, фокус, шапка, подвал — таблица «было → стало», как на пилоте.
   Ширина 390 и 1440 — без горизонтального скролла.

---

## Очередь промптов

1) ~~Закрыть фазу 4 — навигация.~~ Сделано.
2) ~~«PROMPT-23-kejsy-pilot-hoff»~~ — сделано (`38e2e28`): кейс HOFF.
   ~~«PROMPT-25-chistka-video-i-reestr»~~ — сделано: один плеер, валидатор на
   живые данные, чистка мёртвого кода (`/cases`, `portfolio.data.ts`,
   `src/config/routes.ts`). ~~«PROMPT-26-kejs-korona»~~ — сделано: кейс KORONA.
   Sber ждёт ответа: какие 3 из 6 роликов курса монтировал Юрий. Дальше по
   одному кейсу на промпт: Sber Архитектура, ГРК, Альметьевск, СИБУР; VK Fest
   — страницы не существует вообще (см. «Кейсы портфолио» выше); «Учёные
   Нижнего» — после согласования цвета заказчиком.
3) ~~«PROMPT-24-photo-masterklass»~~ — сделано (`0bdc1e0`), хвост (лайтбокс одной
   лентой на 61 кадр, кнопка заявки между блоками) тоже закрыт отдельным фиксом.
4) ~~«PROMPT-27-obo-mne-i-handoff»~~ — сделано: блок «Преподавание и телевидение»,
   перенос этого файла в репозиторий.
5) ~~«PROMPT-28-shrifty»~~ — сделано (`15eff07`): единая система шрифтов, все
   три жалобы закрыты.
6) ~~Правка навигации `/calculator`~~ — сделано (`9681f77`): дропдаун «Услуги»
   из `navigation.data.ts`, «Войти»/`/account` убран из публичной шапки.
7) ~~«PROMPT-29-temnaya-tema-pilot»~~ — сделано: механизм + пилот
   `/video-dlya-marketpleysov`, хвост фазы 3 (вес H1/H2). Дальше — перевод
   остальных 10 статических + 48 V3-страниц по инструкции выше, возможно
   второй моделью в `qwen/site`. Когда все переведены — тёмная тема
   становится умолчанием, светлые значения и `--ds-accent-text-legacy`
   удаляются (отдельная задача, не эта фаза).

## Осталось — по приоритету

### 1. Видимое и функциональное (претензии владельца, названы прямым текстом)
- ~~В верхнем меню нет кучи ссылок~~ — закрыто фазой 4 (навигация).
- ~~Калькулятор закопан~~ — закрыто фазой 4: в шапке, мобильном меню, подвале,
  плюс на `/ceny`, каждой посадочной и в конце статей блога.
- В блоге отсутствуют написанные статьи — розыск проведён (PROMPT-21 этап А):
  из семи тем SEO-ТЗ опубликованы две плюс одна вне списка; для остальных
  четырёх нет фактуры от владельца, статьи не пишутся без неё (ТЗ прямо
  запрещает воду). Очередь тем без материала: «Как организовать трансляцию
  конференции», «Видеограф и оператор-постановщик — в чём разница», «Как
  снимают на действующем производстве», «Как подготовиться к съёмке интервью».
  Фактуру даёт владелец, отдельного `TODO.md` в репозитории заводить не стали
  (решение владельца, PROMPT-27) — очередь тем живёт здесь.
- ~~Заголовок блога «О съёмке и монтаже» ломается на четыре строки по слову~~ —
  закрыто фазой 3: `.v3-editorial-hero h1` был `max-width:8ch`, замер на
  1440px до фикса — 4 строки (409px/100px), после — 2 (203px/100px).
- ~~Шрифты на `/photo` — переписать на брендбук~~ — закрыто фазой 3: h1/h2/h3
  на `var(--ds-font-display)`, тело — `var(--ds-font-body)`.
- ~~`/ceny` — съехал шрифт~~ — закрыто фазой 3: весь текст страницы, включая
  цену, был `ui-monospace` (h1/h2/h3 никогда не задавали свой font-family,
  наследовали от body) — теперь Inter через новое базовое правило
  `h1,h2,h3 { font-family: var(--ds-font-display) }`.
- ~~Нужно больше кликабельности в принципе~~ — закрыто этапом В фазы 4: у
  каждой публичной страницы минимум 2 контекстные входящие + 3 исходящие ссылки.

### 2. Перевод на тёмную тему
Механизм и пилот `/video-dlya-marketpleysov` сделаны (PROMPT-29). Осталось:
~47 светлых страниц (10 статических + все V3), постранично, отдельным
коммитом на страницу, по инструкции в «Тёмная тема: как перевести следующую
страницу» выше. Главная не переведена — тёмная, но до токенов, отдельный заход.
Сюда же переписывание `src/legal/legal.css`: он живёт со своим `:root`
вне системы токенов, это переписывание, а не подмена значений (шрифтовые
токены уже продублированы в PROMPT-28, цветовые — при переводе на тёмное).

### 3. Шесть систем шапки и подвала
Источник данных объединён (фаза 4, `navigation.data.ts`) — правка пункта
меню теперь доезжает до всех систем одним изменением. Визуально системы
всё ещё разные (вёрстку и стиль фаза 4 намеренно не трогала) — сведение
в одну систему визуально осталось отдельной задачей:

| Система | Где | Страниц |
|---|---|---|
| V3App (v3-*) | свои SiteHeader/Footer | 48 |
| Layout.tsx (ds-*) | `src/components/site/Layout.tsx` | 2 |
| site-shell.js | статический HTML + инъекция в рантайме | 11 |
| LegalApp | свои, без общих классов | 8 |
| Калькулятор | свой топбар, подвал уже есть (фаза 4) | 1 |
| Журнал | голый `<nav>`, одна ссылка | 1 (noindex) |

### 4. Машиночитаемость (промпт 19, фазы 2–4)
`llms.txt`, JSON-LD `@graph` из данных, блок «Спросите нейросеть»
с запасной кнопкой «Скопировать вопрос». Приоритет понижен, но не отменён.
См. «План — чтобы нас знали нейросети».

### 5. Страница поисковых запросов
По образцу `/faq` Вовы: не вопросы-ответы, а список запросов с фильтром —
машина по сбору длинного хвоста вместо тридцати посадочных страниц.
См. «Разбор Милосердова — детально по блокам».

### 6. Технический долг
- Сузить `.site-static h2` и убрать два `!important`, которыми чинили плашку кук.
  Это четвёртый случай класса «правило не доживает до рендера» — лечить причину.
- Проверка на сборке «объявлено против вычисленного».
- Аудит тестов, которые проверяют формулировки и захардкоженные числа
  вместо поведения.
- `viewport-fit=cover` — ждёт проверки на айфоне владельцем.
- ~~Редирект `/portfolio/photo` → `/photo`~~ — сделано (фаза 4).
- Перенос формы `/api/send-form` с Vercel на свой Express. **Самая рискованная
  задача проекта** — через неё идут все обращения. Отдельным заходом, не в общей куче.
- Отвязать домен от мёртвого Vercel-проекта и лишние проекты от автодеплоя.

---

## Ждёт владельца

- Фотоархивы папками — за ними стоит весь блок продвижения.
- Три кода подтверждения: Яндекс, Google, Bing.
- Решение по личным данным в `llms.txt`: ИП или физлицо, что выкладывать.
- Список поисковых запросов — вычеркнуть лишнее из чернового.
- Какой бесплатный продукт заменит кнопку «оставить заявку».
- ~~Блок про преподавание и ГТРК на «Обо мне» — да или нет~~ — да, опубликован
  (PROMPT-27).
- Sber Архитектура: какие 3 из 6 роликов курса монтировал Юрий (кейс ждёт ответа).
- Проверить на телефоне надпись первого экрана и переключатель цветокоррекции.

---

## Продвижение — не начато

Авито (четыре карточки готовы с 10.09, ждут фотографий), Яндекс Бизнес
(перезалить прайс), Яндекс Услуги, 2ГИС, HH.

---

## Про старые файлы

`SITE-HANDOFF.md` и `TASKS.md` в папке `site` датированы 23–25 августа
и разошлись с кодом. Не источник истины. Этот файл (`docs/SITE-HANDOFF.md`
в репозитории) — их преемник: перенесён и пересобран владельцем 24.09.2026,
теперь обновляется вместе с кодом в каждом заходе, который меняет сайт
(правило — в `AGENTS.md`).

## Ловушки этого проекта

Это главное, что модель обязана знать до первой правки. Всё перечисленное
уже стреляло, и не по одному разу.

**1) Маршрута нет в пре-рендере — страницы для поиска не существует.**
Сборка зелёная, тесты зелёные, в браузере страница открывается — а робот видит
пустой `<div id="root">`. Так `/ceny` месяц отдавал поисковику семь цен из
семнадцати. Любая правка, добавляющая или переносящая страницу, обязана
пройти через пре-рендер и H1-гейт.

**2) Роутинг ручной.** Компонент `V3App` читает `window.location.pathname`,
router-библиотеки нет. Привычные паттерны React Router здесь просто не работают.

**3) Шесть систем шапки и подвала.** Источник данных теперь один
(`navigation.data.ts`, фаза 4) — но вёрстка всё ещё в шести разных местах,
правка стиля «в шапке» по-прежнему может не доехать до всех систем, если
трогать не токены/данные, а разметку конкретной системы напрямую.
Таблица систем — в «Состояние сайта».

**4) Объявленное не доходит до рендера.** Слои `!important` в `site-skin.css`,
потерянная специфичность, забытая подключённая таблица стилей. Проверять надо
**вычисленный стиль** (`getComputedStyle`), а не исходник. Скриншоты в этом
проекте врали многократно — на них полагаться нельзя.

**5) Второй источник истины.** Захардкоженные цены в разметке, захардкоженные
счётчики в тестах и заголовках, `nth-child` вместо смысловых селекторов,
генерируемые файлы в git. Любая правка, которая заводит второй источник
для одного и того же факта, — брак, даже если внешне работает. Свежие примеры:
`portfolio.data.ts` (мёртвый реестр, валидатор проверял его вместо живого) и
`src/config/routes.ts` (мёртвый параллельный список маршрутов) — оба найдены
и удалены в этом заходе.

**6) `git mv` и `git rm --cached` автоматически стейджат.** Отсюда случайно
склеенные коммиты. Перед каждым коммитом — `git status` глазами.

---

## Ритуал перед каждым коммитом

1. `npm run build` — проходит.
2. `npm test` — проходит.
3. H1-проверка по затронутым маршрутам локально.
4. `git status` — застейджено ровно то, что нужно.
5. В отчёте: что менялось, почему, чем проверено. **Не «сделано», а «проверено тем-то».**
6. Задача, которая меняет сайт, в том же заходе обновляет этот файл: раздел
   «Сделано» и очередь (правило добавлено в `AGENTS.md`, PROMPT-27).

Если что-то из этого не получается — модель останавливается и пишет отчёт,
а не обходит проверку. Обойдённая проверка хуже невыполненной задачи.
